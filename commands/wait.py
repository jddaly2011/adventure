#1/usr/bin/python
def wait(commands, available_actions, player, room):
    if len(commands) > 1:
        print "\tI don't understand wait in that context."
        return

    else:
        print "\tTime passes."
        return

    # if __name__ == "__main__":
    #     trip()
